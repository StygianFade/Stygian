# API Reference

Use this section for stable API contracts:

- `core/` for `stygian.h` public runtime APIs
- `cmd/` for `stygian_cmd.h` command buffer APIs
- `ap/` for `stygian_ap.h` backend abstraction contracts
