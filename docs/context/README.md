# Context Archive

This folder preserves historical and research documents.
Files here are not the active contract unless explicitly referenced.

## Contents

- historical architecture snapshots
- triad and unicode research plans
- benchmark and positioning notes

Use `../README.md` and the active sections (`architecture`, `reference`, `guides`, `perf`, `governance`) for current implementation behavior.
